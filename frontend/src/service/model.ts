export interface ApprovedGame {
    id? : string,
    gameName : string,
    spentMoney : number,
    playtime : number,
    approved : boolean
}

export interface UnapprovedGame {
    id? : string,
    gameName : string,
    spentMoney : number,
    playtime : number,
    approved : boolean
}